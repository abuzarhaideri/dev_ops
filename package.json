{
  "name": "shopsmart",
  "private": true,
  "description": "ShopSmart monorepo — E2E testing with Playwright",
  "scripts": {
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
